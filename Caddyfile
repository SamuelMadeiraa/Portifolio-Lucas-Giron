# HTTPS automático para o domínio do .env (DOMAIN=seudominio.com.br)
# Para testar só pelo IP, sem domínio: DOMAIN=:80 e COOKIE_SECURE=false no .env
{$DOMAIN} {
	encode zstd gzip

	# permite subir vídeos grandes pelo painel
	request_body {
		max_size 600MB
	}

	reverse_proxy site:3000
}
